# Base URL of the Laravel API (without /admin). In dev the Vite proxy forwards /api to http://127.0.0.1:8000.
# In production point it to the absolute API URL, e.g. https://api.example.com/api (and add the dashboard
# origin to DASHBOARD_URL in the backend .env so CORS allows it).
VITE_API_BASE_URL=/api
